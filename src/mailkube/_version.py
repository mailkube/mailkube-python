"""Single source of the package version.

Kept in step with ``project.version`` in ``pyproject.toml`` (semantic-release owns the
bump; this constant is updated by the same release commit).
"""

from __future__ import annotations

__version__ = "0.1.0"
